посмотреть как делать запросы с query-параметрами и скорее всего придется сделать запрос opreation-history с query



1) РАЗОБРАТЬСЯ С ЗАПРОСОМ ИСТОРИЯ ОПЕРАЦИЙ. КАКОЙ Я ТОКЕН ПРИНИМАЮ И КУДА ЕГО СУВАТЬ (ВРОДЕ ПРИНИМАЮ В Bearer в заголовке Authorization, но если 
вписываю в сваггере в body Bearer токен, то не работает, а если access_token который от юмани, то работает)

2) посмотреть почему у меня не создаются query поля в сваггере для запроса получения операций

3) поменять в swagger-output.json примеры данных в запросе

4) в запросе "получение истории операций" добавить все поля в сваггер прямо как в "получение информации о подписке"

5) в предпоследнем запросе с yoomoney сделать body (доработать)